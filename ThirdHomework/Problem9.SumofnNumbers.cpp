#include <iostream>

using namespace std;

int main()
{
    double a,s,sum=0;
    cin >>s;
    for (int i = 0; i <s;i++)
        {
            cin >>a;
        sum = sum + a;
        }
    cout <<sum;
}
